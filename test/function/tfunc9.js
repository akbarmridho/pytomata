function tiit() {
  if (x <= 0) {
    x = 2 + 5;
    x = 25;
    return 0;
  } else if ((x + 4) >>> 3 == 1) {
    if (censored) {
      return 3;
    } else {
      return 2;
    }
  } else if (x == 32) {
    return 2 + x + (x + 6) ? "t" : x + 9 ? "b" : "sss";
  } else {
    return "another censored";
  }
}
